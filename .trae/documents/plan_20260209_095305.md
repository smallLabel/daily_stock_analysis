# 一次性重构：数据获取层 & 数据库操作完整架构

## 实施步骤

### Phase 1: 数据库层重构
1. 创建 `data_provider/database/` 目录结构
2. 迁移 SQLite 连接管理（从 `database.py`）
3. 创建 `data_provider/models/` 目录，迁移 ORM 模型（从 `src/storage.py`）
4. 创建 `data_provider/repository/` 完整实现

### Phase 2: 缓存层重构
1. 创建 `data_provider/cache/` 目录
2. 实现 MemoryCache 和 RedisCache
3. 集成缓存到 Repository

### Phase 3: 清理与兼容
1. 清理废弃文件
2. 更新 `src/storage.py` 为兼容层
3. 验证所有功能正常

## 预计文件变更
- 新建: ~15 个文件
- 修改: ~5 个文件（主要是导入路径更新）
- 删除: ~3 个文件（废弃的重复代码）

## 验证标准
- 所有现有功能正常工作
- 个股分析流程完整可用
- 无 regression 错误